import { OverdueHighlight } from './overdue-highlight';

describe('OverdueHighlight', () => {
  it('should create an instance', () => {
    const directive = new OverdueHighlight();
    expect(directive).toBeTruthy();
  });
});
